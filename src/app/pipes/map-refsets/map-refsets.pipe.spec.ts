import { MapRefsetsPipe } from './map-refsets.pipe';

describe('MapRefsetsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapRefsetsPipe();
    expect(pipe).toBeTruthy();
  });
});
