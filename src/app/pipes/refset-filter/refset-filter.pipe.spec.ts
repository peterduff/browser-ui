import { RefsetFilterPipe } from './refset-filter.pipe';

describe('RefsetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RefsetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
