import { AttributeRefsetsPipe } from './attribute-refsets.pipe';

describe('AttributeRefsetsPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeRefsetsPipe();
    expect(pipe).toBeTruthy();
  });
});
