import { RefsetAlphabeticalPipe } from './refset-alphabetical.pipe';

describe('RefsetAlphabeticalPipe', () => {
  it('create an instance', () => {
    const pipe = new RefsetAlphabeticalPipe();
    expect(pipe).toBeTruthy();
  });
});
