import { SimpleRefsetsPipe } from './simple-refsets.pipe';

describe('SimpleRefsetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleRefsetsPipe();
    expect(pipe).toBeTruthy();
  });
});
