import { AssociationRefsetsPipe } from './association-refsets.pipe';

describe('AssociationRefsetsPipe', () => {
  it('create an instance', () => {
    const pipe = new AssociationRefsetsPipe();
    expect(pipe).toBeTruthy();
  });
});
