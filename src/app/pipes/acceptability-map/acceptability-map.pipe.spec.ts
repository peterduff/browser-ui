import { AcceptabilityMapPipe } from './acceptability-map.pipe';

describe('AcceptabilityMapPipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptabilityMapPipe();
    expect(pipe).toBeTruthy();
  });
});
