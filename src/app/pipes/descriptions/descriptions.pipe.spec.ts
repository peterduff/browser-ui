import { DescriptionsPipe } from './descriptions.pipe';

describe('DescriptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
